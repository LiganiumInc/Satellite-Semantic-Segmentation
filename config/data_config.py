# Data
class data_config:
    
    RAW_DATA_PATH = 'data/DubaiDataset/'           # Folder where to store the dataset 
    PATCHES_DATA_PATH = 'data/save_path/patches/'           # Folder where to store the dataset after patching
    TRAIN_DATA_PATH = 'data/save_path/train/'               # Folder where to store the training dataset
    VAL_DATA_PATH = 'data/save_path/val/'                   # Folder where to store the validation dataset 
    TEST_DATA_PATH = 'data/save_path/test/'                 # Folder where to store the test dataset
    
    
    PATCH_SIZE = 256
    
    TRAIN_SIZE = 0.7
    VAL_SIZE = 0.2
    TEST_SIZE = 0.1
    
    
    RANDOM_STATE = 42
    
    
    